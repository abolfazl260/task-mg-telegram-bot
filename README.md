# TaskBot — ربات مدیریت تسک تلگرام

یک ربات تلگرامی ساده و کاربردی برای مدیریت کارها، جلسات و اقدامات روزمره.

---

## ✨ امکانات فعلی

- ✅ **ایجاد تسک** با عنوان، اولویت اجباری، ددلاین، دسته‌بندی و تگ
- 🎯 **اولویت‌بندی** کارها (بالا 🔴 / متوسط 🟠 / پایین 🟢)
- 📅 **تعیین زمان انجام** (فردا، ۳ روز، ۱ هفته، ۲ هفته، ۳ هفته یا تاریخ دلخواه)
- 📋 **مشاهده لیست تسک‌های فعال** با صفحه‌بندی
- ⏳ **محاسبه زمان باقی‌مانده** تا ددلاین
- 📊 **داشبورد وضعیت اولویت‌ها**
- 📥 **دانلود خروجی اکسل** از تسک‌های فعال
- 🗓️ پشتیبانی از **تاریخ شمسی (جلالی)** در گزارش‌ها
- 💾 ذخیره‌سازی ساده و سبک با فایل **CSV**
- 👥 **Guest Mode تلگرام** برای ثبت سریع تسک و ارسال گزارش مهم از هر گروه، بدون اضافه کردن بات به گروه
- 💙 **دونیت با Telegram Stars** در مبالغ ۱۰، ۴۰ و ۱۰۰ استارز
- 🌱 دستور `/habit` برای مدیریت عادت‌ها
- 🤖 دستور `/ai` برای پرسیدن سؤال فارسی درباره تسک‌ها با Groq
- 🧑‍💼 **Secretary Mode / Business Connection** برای دریافت پیام‌های چت‌های مدیریت‌شده و پاسخ‌گویی مجاز از طرف مالک حساب
- 🔗 **اتصال به Jira** برای هر کاربر و هر ربات
- 🔄 **همگام‌سازی دوطرفه Telegram ↔ Jira** برای ایجاد و به‌روزرسانی تسک‌ها

---

## 🚀 شروع سریع

### پیش‌نیازها
- Python 3.10 یا بالاتر
- یک ربات تلگرام (از [@BotFather](https://t.me/BotFather) توکن بگیرید)

### نصب

```bash
git clone https://github.com/abolfazl260/task-mg-telegram-bot.git
cd task-mg-telegram-bot
pip install -r requirements.txt
```

> **نکته:** کتابخانه `jdatetime` برای نمایش تاریخ شمسی استفاده می‌شود. در صورت نیاز آن را هم نصب کنید:
> ```bash
> pip install jdatetime
> ```

### تنظیمات

فایل `.env` را در ریشه پروژه بسازید و توکن ربات را قرار دهید:

```env
BOT_TOKEN=123456:ABC-DEF...
# اختیاری: فعال‌سازی دستیار هوشمند /ai با Groq
GROQ_API_KEY=gsk_...
GROQ_MODEL=openai/gpt-oss-20b
```

### اجرا

```bash
python main.py
```

### پلتفرم مدیریت چند ربات

این پروژه می‌تواند به جای یک ربات ثابت، چند ربات تلگرام را با یک هسته مشترک اجرا کند. برای هر ربات یک فایل پروفایل جدا در پوشه `bots/` بسازید و توکن و یوزرنیم همان ربات را فقط در `.env` نگه دارید؛ هیچ توکنی نباید داخل کد یا فایل JSON قرار بگیرد.

نمونه `.env` برای اجرای چند ربات:

```env
BOT_PROFILES=task_manager,request_workflow
BOT_TASK_MANAGER_TOKEN=123456:AAA...
BOT_TASK_MANAGER_USERNAME=TaskManagerPersian_Bot
BOT_REQUEST_WORKFLOW_TOKEN=987654:BBB...
BOT_REQUEST_WORKFLOW_USERNAME=RequestApproval_Bot
```

برای هر نام داخل `BOT_PROFILES` باید یک فایل هم‌نام در مسیر `bots/<name>.json` وجود داشته باشد. دو نمونه آماده در فایل‌های `bots/task_manager.json.example` و `bots/request_workflow.json.example` قرار دارد؛ آن‌ها را کپی کنید، پسوند `.example` را بردارید و قابلیت‌ها، منو و workflow اختصاصی ربات را تنظیم کنید.

هر پروفایل ربات می‌تواند این موارد را مستقل تعریف کند:

- نام، توضیحات، وضعیت فعال/غیرفعال و متغیرهای محیطی توکن/یوزرنیم
- قابلیت‌های فعال مانند تسک، تیم، گزارش، عادت، AI، دونیت، Guest Mode و import گروهی
- تنظیمات اختصاصی و سطح دسترسی‌ها
- برچسب‌های workflow و دکمه‌های اکشن اختصاصی
- منوی اصلی متفاوت برای journey هر ربات

اگر `BOT_PROFILES` تنظیم نشود، برنامه مثل قبل فقط با `BOT_TOKEN` و `BOT_USERNAME` یک ربات پیش‌فرض اجرا می‌کند.

### ساخت ربات اختصاصی توسط کاربران

از منوی اصلی گزینه «🤖 ساخت ربات اختصاصی» اضافه شده است. هر کاربر می‌تواند قابلیت‌های مورد نیاز ربات خود را انتخاب کند و سپس توکن Bot API دریافت‌شده از `@BotFather` را ارسال کند. درخواست در `data/custom_bots.csv` با وضعیت `active` و پلن `free_beta` ذخیره می‌شود؛ بنابراین این قابلیت در نسخه فعلی رایگان است و بعداً می‌تواند به پلن‌های هزینه‌ای متصل شود.

قابلیت‌های قابل انتخاب شامل مدیریت تسک، تیم‌ها، تمپلیت‌ها، عادت‌ها، گزارشات، جستجو، ورود گروهی، دستیار هوشمند و Guest Mode است. ربات‌های ثبت‌شده کاربر با کلید اختصاصی خود از داده‌های سایر ربات‌ها جدا می‌شوند و در اجرای بعدی سرویس به لیست پروفایل‌های فعال اضافه می‌شوند.

> نکته امنیتی: توکن‌های کاربران در فایل داده محلی ذخیره می‌شوند؛ در محیط production دسترسی این فایل را محدود کنید و در صورت نیاز آن را به secret manager یا دیتابیس رمزنگاری‌شده منتقل کنید.

### اتصال و همگام‌سازی Jira

هر کاربر می‌تواند **Jira سازمان خودش** را به همین ربات متصل کند. اتصال در سطح `Telegram User + Bot` نگهداری می‌شود؛ بنابراین اتصال یک کاربر در یک ربات با اتصال او در ربات دیگر تداخل ندارد.

قابلیت‌های فعلی:

- اتصال به Jira Cloud با URL سازمان، ایمیل، API Token و Project Key
- اعتبارسنجی اتصال قبل از ذخیره‌سازی
- ایجاد Issue در Jira وقتی کاربر Task جدیدی در Telegram دارد
- دریافت Issueهای Jira که به کاربر متصل اختصاص داده شده‌اند و ساخت Task متناظر در Telegram
- به‌روزرسانی عنوان، توضیحات، وضعیت، اولویت و Deadline بین دو طرف
- نگهداری ارتباط پایدار بین `Task ID` و `Jira Issue Key`
- همگام‌سازی خودکار هر ۶۰ ثانیه
- امکان قطع اتصال با `/jira_disconnect`
- مشاهده وضعیت اتصال با `/jira_status`

#### نحوه اتصال مرحله‌به‌مرحله

1. **ورود به ربات** و اجرای دستور زیر:

```text
/jira
```

2. ربات آدرس Jira سازمان را می‌پرسد. مثال:

```text
https://company.atlassian.net
```

3. ایمیل حساب Jira را وارد کنید؛ همان حسابی که باید Issueهای آن همگام شوند.

4. از حساب Atlassian خود یک **API Token** بسازید و آن را در مرحله بعد ارسال کنید.

5. کلید پروژه Jira را وارد کنید؛ مثلاً:

```text
PROJ
```

6. ربات ابتدا با Jira احراز هویت را بررسی می‌کند و دسترسی به Project را تست می‌کند.

7. در صورت موفقیت، اتصال ذخیره و Sync خودکار فعال می‌شود.

> ⚠️ API Token را فقط در چت خصوصی با ربات ارسال کنید. ربات تلاش می‌کند پیام حاوی Token را بلافاصله حذف کند. Token در فایل اتصال محلی ذخیره می‌شود؛ برای محیط Production بهتر است این اطلاعات در Secret Manager یا ذخیره‌سازی رمزنگاری‌شده نگهداری شوند.

#### چرخه همگام‌سازی

```text
Telegram Task
     │
     ├── ایجاد شد ───────────────→ Jira Issue ساخته می‌شود
     │
     ├── عنوان/توضیحات تغییر کرد ─→ Jira Issue به‌روزرسانی می‌شود
     │
     ├── وضعیت تغییر کرد ─────────→ Jira Transition اجرا می‌شود
     │
     └── Deadline تغییر کرد ──────→ Jira Due Date به‌روزرسانی می‌شود

Jira Issue
     │
     ├── ایجاد/کشف شد ────────────→ Task در Telegram ایجاد می‌شود
     │
     ├── Summary تغییر کرد ───────→ عنوان Task تغییر می‌کند
     │
     ├── Description تغییر کرد ───→ توضیحات Task تغییر می‌کند
     │
     ├── Status تغییر کرد ────────→ وضعیت Task تغییر می‌کند
     │
     ├── Priority تغییر کرد ──────→ اولویت Task تغییر می‌کند
     │
     └── Due Date تغییر کرد ──────→ Deadline Task تغییر می‌کند
```

#### مثال

اگر کاربر در Telegram این Task را بسازد:

```text
پیگیری قرارداد مشتری X
اولویت: بالا
Deadline: 2026-08-20
```

در Sync بعدی یک Issue در Project متصل Jira ساخته می‌شود و ارتباط آن با Task محلی ذخیره می‌شود.

اگر مدیر در Jira عنوان یا وضعیت همان Issue را تغییر دهد، در Sync بعدی Task Telegram نیز به‌روزرسانی می‌شود.

اگر کاربر در Telegram وضعیت Task را به `Done` تغییر دهد، Sync بعدی وضعیت Issue Jira را نیز به وضعیت متناظر منتقل می‌کند.

#### دستورات Jira

| دستور | توضیح |
|---|---|
| `/jira` | شروع اتصال Jira |
| `/jira_status` | مشاهده وضعیت اتصال و آخرین Sync |
| `/jira_disconnect` | قطع اتصال Jira |
| `/cancel` | لغو فرآیند اتصال در حال انجام |

> نکته: نسخه فعلی Sync برای Jira Cloud طراحی شده و Issueهای اختصاص‌یافته به حساب متصل را همگام می‌کند. در پروژه‌های Jira Server/Data Center ممکن است روش احراز هویت و endpointها متفاوت باشد.

### Secretary Mode تلگرام

برای اتصال ربات به حساب کاربر و پردازش پیام‌های چت‌های مدیریت‌شده، ابتدا در [@BotFather](https://t.me/BotFather) قابلیت **Secretary Mode** را برای ربات فعال کنید. بعد از اتصال کاربر، ربات آپدیت‌های `business_connection`، `business_message`، `edited_business_message` و `deleted_business_messages` را دریافت و ثبت می‌کند.

رفتار پیاده‌سازی‌شده:

- آخرین وضعیت هر Business Connection شامل `can_reply` و `is_enabled` در `data/business_connections.json` ذخیره می‌شود.
- پیام‌های Business، ویرایش‌ها و حذف‌ها برای پردازش بعدی در همان فایل ثبت می‌شوند.
- اگر کاربر در یک چت مدیریت‌شده پیام `/secretary_status` بفرستد و اتصال اجازه پاسخ داشته باشد، ربات با `business_connection_id` از طرف حساب Business پاسخ می‌دهد.
- پاسخ خودکار عمومی به‌صورت پیش‌فرض خاموش است تا ربات بدون تصمیم مالک حساب به مشتریان پاسخ ندهد. برای فعال‌سازی آن در `.env` بنویسید:

```env
SECRETARY_AUTO_REPLY_ENABLED=true
SECRETARY_AUTO_REPLY_TEXT=پیام شما دریافت شد؛ به‌زودی پاسخ می‌دهیم.
```

لینک عمیق `/start bizChat<user_chat_id>` که از نوار «Manage Bot» تلگرام می‌آید، بدون تداخل با لینک‌های دعوت تیم پذیرفته می‌شود و منوی اصلی ربات را نمایش می‌دهد.

### دستیار هوشمند Groq

اگر `GROQ_API_KEY` در `.env` تنظیم شده باشد، می‌توانید از دستور `/ai` برای تحلیل تسک‌های خود استفاده کنید:

```text
/ai امروز روی چه کاری تمرکز کنم؟
/ai تسک‌های عقب‌افتاده من را اولویت‌بندی کن
```

کلید API را داخل کد قرار ندهید؛ فقط آن را به‌صورت متغیر محیطی یا در فایل `.env` محلی نگه دارید. آدرس پیش‌فرض Groq روی endpoint سازگار با OpenAI یعنی `https://api.groq.com/openai/v1/responses` تنظیم شده است.

### ثبت تسک بدون جوین بات به گروه (Guest Mode)

برای اینکه کاربران بتوانند بات را در هر گروهی صدا بزنند و بدون اضافه کردن بات به گروه تسک بسازند، در BotFather Mini App گزینه **Guest Mode** را برای بات فعال کنید. بعد از فعال‌سازی، کاربر می‌تواند در گروه بنویسد:

```text
@YourBot add پیگیری قرارداد 2026-08-20 فوری
```

بات همان‌جا یک پاسخ مهمان می‌دهد و تسک را برای همان کاربر با دسته‌بندی `Guest: نام گروه` ثبت می‌کند.

برای ارسال گزارش مهم در گروه نیز می‌توانید از همان Guest Mode استفاده کنید:

```text
@YourBot گزارش مهم
@YourBot report status
```

خروجی شامل تعداد کل تسک‌ها، تسک‌های فعال، اولویت‌های بالا، موارد موعد امروز و تسک‌های عقب‌افتاده است و به‌صورت پاسخ مهمان داخل همان گروه نمایش داده می‌شود.

#### مراحل کامل فعال‌سازی و استفاده از Guest Mode در گروه‌ها

1. وارد [@BotFather](https://t.me/BotFather) شوید و ربات خود را انتخاب کنید.
2. از تنظیمات Mini App / قابلیت‌های ربات، **Guest Mode** را فعال کنید.
3. مطمئن شوید متغیر `BOT_USERNAME` در `.env` با یوزرنیم ربات شما تنظیم شده است تا مثال‌ها و لینک‌ها درست باشند.
4. ربات لازم نیست عضو گروه باشد؛ کاربر در هر گروهی نام کاربری ربات را تایپ می‌کند و سپس متن تسک یا گزارش را می‌نویسد.
5. برای ساخت تسک، الگوی `@YourBot add عنوان تسک تاریخ اولویت` را بفرستید؛ برای گزارش مهم، `@YourBot گزارش مهم` را ارسال کنید.
6. گزارش‌ها و تسک‌های ساخته‌شده به حساب همان کاربری وصل می‌شوند که ربات را در گروه صدا زده است.

### اجرای خودکار بعد از ری‌استارت سرور

برای اینکه ربات بعد از ری‌استارت شدن سرور، کرش برنامه یا قطع موقت شبکه دوباره به صورت خودکار اجرا شود، می‌توانید سرویس `systemd` آماده پروژه را نصب کنید. این روش برای سرورهای لینوکسی رایج مانند Ubuntu و Debian پیشنهاد می‌شود.

1. پروژه را روی سرور کپی کنید و در ریشه پروژه دستور زیر را اجرا کنید:

```bash
sudo ./scripts/install-systemd-service.sh
```

2. اگر فایل `.env` در مسیر نصب ساخته شد، مقدار `BOT_TOKEN` را در آن تنظیم کنید:

```bash
sudo nano /opt/task-mg-telegram-bot/.env
```

3. سرویس را اجرا کنید:

```bash
sudo systemctl start task-mg-telegram-bot
```

4. وضعیت و لاگ‌ها را بررسی کنید:

```bash
sudo systemctl status task-mg-telegram-bot
sudo journalctl -u task-mg-telegram-bot -f
```

سرویس نصب‌شده با `Restart=always` تنظیم شده است؛ بنابراین اگر برنامه متوقف شود یا سرور ری‌استارت شود، `systemd` ربات را دوباره اجرا می‌کند. برای تغییر مسیر نصب، کاربر سرویس یا نسخه پایتون می‌توانید متغیرهای زیر را قبل از اجرای اسکریپت مقداردهی کنید:

```bash
sudo INSTALL_DIR=/opt/task-mg-telegram-bot SERVICE_USER=taskbot PYTHON_BIN=python3 ./scripts/install-systemd-service.sh
```

---

## 📌 دستورات ربات

| دستور | توضیح |
|---|---|
| `/start` | شروع ربات و نمایش منوی اصلی |
| `/add` | افزودن تسک جدید |
| `/tasks` | مشاهده لیست تسک‌های فعال |
| `/skip` | رد کردن فیلد اختیاری (دسته‌بندی یا تگ) |
| `/habit` | مدیریت عادت‌ها |
| `/donate` | حمایت با ۱۰، ۴۰ یا ۱۰۰ استارز |
| `/ai` | دستیار هوشمند تحلیل تسک‌ها با Groq |
| `/jira` | اتصال Jira |
| `/jira_status` | وضعیت اتصال Jira |
| `/jira_disconnect` | قطع اتصال Jira |

---

## 🗂️ ساختار پروژه

```text
task-mg-telegram-bot/
├── main.py                 # نقطه ورود اصلی ربات
├── config.py               # تنظیمات و بارگذاری توکن
├── requirements.txt
├── .env                    # توکن ربات (در گیتignore قرار گیرد)
├── data/
│   ├── tasks.csv           # ذخیره‌سازی تسک‌ها
│   ├── jira_connections.json # اتصال‌های Jira کاربران
│   └── jira_task_links.json # نگاشت Taskهای Telegram به Issueهای Jira
├── handlers/
│   ├── start.py            # دستور /start
│   ├── menu.py             # منوی اصلی و دکمه‌ها
│   ├── task.py             # منطق ایجاد و لیست تسک‌ها
│   └── jira.py             # فرآیند اتصال Jira
├── services/
│   ├── csv_manager.py      # خواندن و نوشتن CSV
│   ├── task_service.py     # سرویس ایجاد و دریافت تسک
│   ├── jira_service.py     # API و همگام‌سازی دوطرفه Jira
│   └── excel_service.py    # ساخت فایل اکسل
└── utils/
    └── keyboard.py         # کیبوردهای اولویت و ددلاین
```

---

## 🗺️ نقشه راه (Roadmap)

- [ ] پیاده‌سازی بخش **آمار** (Stats)
- [ ] پیاده‌سازی بخش **تنظیمات**
- [ ] امکان تغییر وضعیت تسک (شروع / انجام شد / لغو)
- [ ] یادآوری خودکار نزدیک ددلاین
- [ ] پشتیبانی از چند کاربر با دسترسی بهتر
- [x] اتصال و همگام‌سازی دوطرفه با **Jira Cloud**
- [ ] اتصال به **Google Calendar**
- [ ] انتقال ذخیره‌سازی به دیتابیس (SQLite / PostgreSQL)

---

## 📄 لایسنس

این پروژه تحت لایسنس [Apache License 2.0](LICENSE) منتشر شده است.
